name: nyc_taxi_intelligence
version: 1.0.0
config-version: 2

profile: nyc_taxi_intelligence

model-paths:
  - models

test-paths:
  - tests

macro-paths:
  - macros

clean-targets:
  - target
  - dbt_packages

models:
  nyc_taxi_intelligence:
    staging:
      +materialized: table
      +schema: staging
    core:
      +materialized: table
      +schema: core
    marts:
      +materialized: table
      +schema: marts

