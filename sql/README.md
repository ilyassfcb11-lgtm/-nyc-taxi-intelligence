# SQL Folder

This folder will contain SQL used for cleaning, modeling, and KPI creation.

Early project SQL will be simple and direct. Later, much of this logic will move into dbt models.

Planned SQL areas:

- staging queries
- fact and dimension queries
- KPI mart queries

